# Legacy / Dead Tests

`main.lisp` references functions not present in the current Lisp SDK
(`dcf-group-peers`, `dcf-simulate-failure`, `dcf-visualize-topology`).

Preserved for reference. Do not load without implementing the missing APIs.