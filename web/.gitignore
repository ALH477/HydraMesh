# SPDX-License-Identifier: LGPL-3.0-only
node_modules/
dist/
bridge/target/
# wasm-pack output + the base64-inlined wasm are build artifacts: the whole dir
# is regenerated by `npm run build` (wasm-pack → inline-wasm.mjs). wasm-ipc.ts
# imports ./wasm/dcf_codec_wasm.js, which exists only after a build.
src/wasm/
